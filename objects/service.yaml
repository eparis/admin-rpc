apiVersion: v1
kind: Service
metadata:
  labels:
    app: remote-shell
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: service-serving-cert
  name: remote-shell
spec:
  ports:
  - name: grpc
    port: 443
    targetPort: grpc
  selector:
    app: remote-shell
